type Cards
  @model @auth(rules: [ {allow: owner } ]) 
  {
  id: ID!
  courseName: String
  contentName: String!
  hostName: String
  date: String!
  time:String
  meetingLink: String!
  capacity: String!
}

type Todo @model @auth(rules: [ {allow: owner } ]) {
  id: ID!
  name: String!
  description: String
}

  